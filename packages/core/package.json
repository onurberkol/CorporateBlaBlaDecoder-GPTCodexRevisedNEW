{
  "name": "@corporate-blabla/core",
  "version": "0.1.0",
  "description": "Shared IP core: tone definitions, golden few-shot libraries, and the Decoder/Composer system-prompt builders.",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.19.0"
  }
}
